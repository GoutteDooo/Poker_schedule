import React from 'react';
import Entete from './Entete';
import Logo from './Logo';

const Header = () => {
    return (
        <header>
          <Entete />
          <Logo />
        </header>
    );
};

export default Header;